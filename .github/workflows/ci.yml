name: ci

on:
  push:
    branches: [main]
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 24
      - name: Validate the skill catalog
        run: bash scripts/validate-skills.sh
      - name: Verify skill references resolve
        run: bash scripts/check-skill-refs.sh
      - name: Verify relative links resolve
        run: bash scripts/check-links.sh
      - name: Catalog SSOT drift-guard
        run: node scripts/check-catalog-sync.cjs
      - name: Deploy-home SSOT drift-guard
        run: node scripts/check-deploy-home.cjs
      - name: Localized README bold-link roster drift-guard (warn-only)
        run: node scripts/check-readme-sync.cjs
